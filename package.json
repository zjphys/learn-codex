{
  "name": "learn-codex-marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "Repository marketplace for the Learn with Codex plugin",
  "engines": { "node": ">=22" },
  "scripts": { "test": "npm --prefix plugins/learn-codex test" }
}
